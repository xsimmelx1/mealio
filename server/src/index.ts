/**
 * Mealio Backend — Platzhalter (Milestone 1).
 * Das vollständige Express-Grundgerüst (/health, /generate-plan, llmClient,
 * CORS, Rate-Limiting) folgt in Milestone 4.
 */
const PORT = Number(process.env.PORT ?? 8787);

console.log(`[mealio-server] Platzhalter aktiv. Express-Grundgerüst kommt in M4. (PORT=${PORT})`);
